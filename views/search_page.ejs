<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Result Management System - Search</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        text-align: center;
        margin: 0;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        background-color: #f2f2f2;
      }

      nav {
        background-color: #333;
        padding: 15px;
        text-align: center;
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 100%;
      }

      nav a {
        color: white;
        text-decoration: none;
        padding: 15px;
        margin: 10px;
        font-weight: bold;
        font-size: 18px;
      }

      form {
        width: 80%;
        max-width: 400px;
        margin-bottom: 20px;
        text-align: left;
      }

      label {
        display: block;
        margin-bottom: 8px;
      }

      input {
        width: 100%;
        padding: 8px;
        margin-bottom: 16px;
        box-sizing: border-box;
      }

      .search-button,
      .clear-button,
      .back-button {
        padding: 10px 20px;
        font-size: 16px;
        cursor: pointer;
        text-decoration: none;
        color: #fff;
        border: none;
        border-radius: 5px;
        transition: background-color 0.3s;
      }

      .search-button {
        background-color: goldenrod;
      }

      .clear-button {
        margin-left: 10px;
        background-color: goldenrod;
      }

      .back-button {
        margin-left: 10px;
        background-color: goldenrod;
      }

      .search-button:hover,
      .clear-button:hover,
      .back-button:hover {
        background-color: #d4af37;
      }

      .header {
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 20px;
        margin-top: 20px;
      }

      .logout-btn {
        background-color: #d9534f;
        border-radius: 5px;
        padding: 10px 20px;
        text-decoration: none;
        color: white;
        transition: background-color 0.3s;
      }

      .logout-btn:hover {
        background-color: #c9302c;
      }
    </style>
  </head>

  <body>
    <nav>
      <a href="/">Result Management System</a>
      <a href="/" class="logout-btn">Logout</a>
    </nav>
    <div class="header">Find Result</div>
    <form id="searchForm">
      <label for="rollNo">Roll No.:</label>
      <input type="text" id="rollNo" name="rollNo" />

      <label for="name">Name:</label>
      <input type="text" id="name" name="name" />

      <button class="search-button" onclick="search()">Search</button>
      <button type="button" class="clear-button" onclick="clearForm()">
        Clear
      </button>
      <a href="/view-records" class="back-button">Back</a>
    </form>

    <script>
      function search() {
        const formData = new FormData(document.getElementById("searchForm"));
        const url = `/search?${new URLSearchParams(formData).toString()}`;
        window.location.href = url;
      }

      function clearForm() {
        const form = document.getElementById("searchForm");
        form.reset();
      }
    </script>
  </body>
</html>
